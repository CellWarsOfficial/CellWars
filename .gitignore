# exclude objects
*.o
# exclude binary folders
bin/
# exclude back-up files
*~
# exclude hidden files(dot files)
.*
# except gitignore itself
!.gitignore
# exclude log files
*.l
*.log
# exclude nohup accidental files
nohup.out
# exclude html output folder
client/html/
# exclude build obtained by react
client/build/
# exclude node dependencies
node_modules/
# exclude node package lock
package-lock.json

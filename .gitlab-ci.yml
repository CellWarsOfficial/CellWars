stages:
    - build
    - test
    - cleanup
    - deploy

build_job:
    stage: build
    script:
        - ./depend.sh
        - make

test_job:
    stage: test
    only:
        - master
    script:
        - make check

cleanup_job:
    stage: cleanup
    script:
        - make clean
    when: always

deploy_job:
    stage: deploy
    only:
        - master
        - ci
    script:
        - apt-get update && apt-get install -y netcat
        - make pings
